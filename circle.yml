machine:
  ruby:
    version: 2.2.3

dependencies:
  pre:
    - sh bin/setup.sh

deployment:
  production:
    branch: master
    commands:
      - sh bin/deploy.sh

test:
  override:
    - echo "No Tests"

